{
  "$schema": "https://opencode.ai/config.json",
  "model": "{{MODEL}}",
  "agent": {
    "build": {
      "mode": "primary",
      "model": "{{MODEL}}",
      "prompt": "{file:./AGENTS.md}"
    }
  }
}
