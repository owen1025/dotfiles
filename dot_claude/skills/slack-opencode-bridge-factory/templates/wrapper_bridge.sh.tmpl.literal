#!/bin/bash
# Auto-generated by slack-opencode-bridge-factory
# DO NOT EDIT — regenerate with: omo-bridge update {{AGENT_NAME}}

# Ensure Homebrew bin is in PATH (launchd/systemd strip it)
export PATH="{{BREW_PATH}}:${PATH}"

set +e
source "${HOME}/.zshrc.local" 2>/dev/null
set -e

source "{{ENV_FILE}}"

mkdir -p "{{LOG_DIR}}"

cd "{{PROJECT_DIR}}"

exec "{{PYTHON_BIN}}" -m bridge.bridge
